# ============================================
# RADIM BRAIN + CHAT v3.1 - Heroku Requirements
# Claude AI + Web Search + PostgreSQL
# ============================================

# Flask & Extensions
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
python-socketio==5.10.0

# Server (eventlet pro WebSocket)
gunicorn==21.2.0
eventlet==0.34.2

# HTTP requests (pro Azure REST API, Gemini, WordPress)
requests==2.31.0

# Database - PostgreSQL (Heroku Postgres)
# Falls back to SQLite if DATABASE_URL not set
psycopg2-binary==2.9.9

# Push Notifications
pywebpush==1.14.0

# Cloudinary (media upload)
cloudinary==1.41.0

# Utilities
python-dotenv==1.0.0

# 🤖 AI PROVIDERS
# Anthropic Claude - Primary AI (Web Search enabled)
anthropic>=0.40.0

# Google Gemini - Fallback (optional)
# google-generativeai>=0.3.0

# 📞 Twilio Voice (phone calls for seniors)
twilio>=9.0.0
