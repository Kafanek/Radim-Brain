# ============================================
# RADIM BRAIN + CHAT v3.1 - Heroku Requirements
# Claude AI + Web Search + PostgreSQL
# ============================================

# Flask & Extensions
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
python-socketio==5.10.0

# Server (eventlet pro WebSocket)
gunicorn==21.2.0
eventlet==0.34.2

# HTTP requests (pro Azure REST API, Gemini, WordPress)
requests==2.31.0

# Database - PostgreSQL (Heroku Postgres)
# Falls back to SQLite if DATABASE_URL not set
psycopg2-binary==2.9.9

# Push Notifications
pywebpush==1.14.0

# Cloudinary (media upload)
cloudinary==1.41.0

# Utilities
python-dotenv==1.0.0

# 🤖 AI PROVIDERS
# Anthropic Claude - Primary AI (Web Search enabled)
anthropic>=0.40.0

# Google Gemini - Fallback (optional)
# google-generativeai>=0.3.0

# 📞 Twilio Voice (phone calls for seniors)
twilio>=9.0.0

# ⏰ Background Scheduler (task reminders push notifications)
APScheduler==3.10.4

# 🏠 Home Assistant + Smart Home
homeassistant_api>=5.0.0
paho-mqtt>=2.0.0
python-kasa>=0.7.0
yeelight>=0.7.0

# 🎙️ Voice filter is SSML-based (no audio processing deps needed)
flask-compress==1.15

# 🌐 Browser Agent v1 — safe read-only web browsing (v10.34)
# trafilatura: state-of-the-art article text extraction
# beautifulsoup4 + lxml: HTML parsing + link extraction
trafilatura>=1.6.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
